spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/postgres
    username: postgres
    password: pass
    driver-class-name: org.postgresql.Driver
  application:
    name:
      EventScope

server:
  port: 8082

logging:
  level:
    org:
      springframework:
        security: DEBUG
        jdbc: DEBUG