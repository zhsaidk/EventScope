spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/postgres
    username: postgres
    password: pass
    driver-class-name: org.postgresql.Driver
  application:
    name:
      EventScope
